# 🚀 Инструкция по установке и запуску

## Важно! Порядок установки

Проект использует **кодогенерацию** через sqlc, поэтому некоторые файлы создаются автоматически при первом запуске. Следуйте этой инструкции по порядку.

---

## Шаг 1: Клонирование проекта

```bash
git clone https://github.com/Soundveyve/fiber-backend.git
cd fiber-backend
```

---

## Шаг 2: Создание .env файла

```bash
cp .env.example .env
```

Откройте `.env` и при необходимости измените настройки (по умолчанию настроено для локальной разработки).

---

## Шаг 3: Установка инструментов

### 3.1 Установка Go (если ещё не установлен)

**macOS:**
```bash
brew install go
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install golang-go
```

**Windows:**
Скачайте установщик с https://go.dev/dl/

Проверьте установку:
```bash
go version
# Должно вывести: go version go1.21.x или выше
```

### 3.2 Установка Docker и Docker Compose

**macOS:**
```bash
brew install --cask docker
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install docker.io docker-compose
sudo systemctl start docker
sudo usermod -aG docker $USER
# Перелогиньтесь после этой команды
```

**Windows:**
Скачайте Docker Desktop с https://www.docker.com/products/docker-desktop

### 3.3 Установка инструментов Go

```bash
make install-tools
```

Эта команда установит:
- `golang-migrate` - для миграций БД
- `sqlc` - для генерации Go кода из SQL

---

## Шаг 4: Запуск базы данных

```bash
make docker-up
```

Эта команда запустит PostgreSQL в Docker контейнере. Подождите 5-10 секунд пока БД полностью запустится.

Проверьте что БД запущена:
```bash
docker ps
# Должен показать контейнер fiber_postgres
```

---

## Шаг 5: Применение миграций

```bash
make migrate-up
```

Эта команда создаст таблицы в базе данных.

---

## Шаг 6: Генерация кода (ВАЖНО!)

```bash
make sqlc
```

Эта команда создаст файлы в `internal/repository/`:
- `db.go` - интерфейс для работы с БД
- `models.go` - структуры моделей
- `users.sql.go` - методы для работы с пользователями

---

## Шаг 7: Установка Go зависимостей

Теперь, когда весь код сгенерирован, установим зависимости:

```bash
go mod tidy
```

Команда `go mod tidy` удалит неиспользуемые зависимости и добавит недостающие.

---

## Шаг 8: Запуск приложения

```bash
make run
```

Или напрямую:
```bash
go run cmd/api/main.go
```

Приложение запустится на **http://localhost:3000**

---

## Проверка работы

### Проверка Health Check

```bash
curl http://localhost:3000/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "services": {
    "api": "healthy",
    "database": "healthy"
  },
  "version": "1.0.0"
}
```

### Создание тестового пользователя

```bash
curl -X POST http://localhost:3000/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "username": "testuser",
    "password": "password123",
    "first_name": "Тест",
    "last_name": "Тестов"
  }'
```

---

## Решение проблем

### Ошибка: "no matching versions for query latest"

**Причина**: Go пытается скачать внутренние пакеты проекта как внешние зависимости.

**Решение**: Сначала сгенерируйте код, затем запустите `go mod tidy`.

```bash
# 1. Примените миграции
make migrate-up

# 2. Сгенерируйте код
make sqlc

# 3. Обновите зависимости
go mod tidy

# 4. Запустите приложение
make run
```

### Ошибка: "migrate: command not found"

**Причина**: golang-migrate не установлен.

**Решение**:
```bash
make install-tools
# Убедитесь что $GOPATH/bin в PATH
export PATH=$PATH:$(go env GOPATH)/bin
```

### Ошибка: "sqlc: command not found"

**Причина**: sqlc не установлен.

**Решение**:
```bash
make install-tools
```

### Ошибка подключения к БД

**Причина**: PostgreSQL не запущен или порт занят.

**Решение**:
```bash
# Проверьте что PostgreSQL запущен
docker ps

# Если не запущен
make docker-up

# Если порт 5432 занят
lsof -i :5432
# Остановите процесс или измените порт в .env
```

### Полный сброс (если что-то пошло не так)

```bash
# 1. Остановите все контейнеры
make docker-down

# 2. Удалите volumes
docker volume rm fiber-backend_postgres_data

# 3. Удалите сгенерированные файлы
rm -rf internal/repository/*.go

# 4. Начните заново с шага 4
make docker-up
make migrate-up
make sqlc
go mod tidy
make run
```

---

## Полезные команды

```bash
make help          # Показать все доступные команды
make dev           # Быстрый старт (БД + миграции)
make run           # Запустить приложение
make test          # Запустить тесты
make build         # Собрать бинарный файл
make docker-logs   # Посмотреть логи контейнеров
```

Удачи в изучении Golang! 🚀
