# Golang Fiber Backend Project

Учебный проект для освоения Golang backend разработки с использованием:
- **Fiber** - веб-фреймворк
- **PostgreSQL** - база данных
- **sqlc** - кодогенерация для SQL запросов
- **golang-migrate** - миграции БД
- **Docker** - контейнеризация

**GitHub**: https://github.com/Soundveyve/fiber-backend

## ⚠️ Важно перед началом

Проект использует **кодогенерацию** (sqlc), поэтому некоторые файлы создаются автоматически.

**📖 Полная инструкция по установке**: [INSTALLATION.md](INSTALLATION.md)

## Быстрый старт

```bash
# 1. Клонируйте проект
git clone https://github.com/Soundveyve/fiber-backend.git
cd fiber-backend

# 2. Установите инструменты
make install-tools

# 3. Запустите БД и примените миграции
make dev

# 4. Сгенерируйте код (ВАЖНО!)
make sqlc

# 5. Установите зависимости
go mod tidy

# 6. Запустите приложение
make run
```

Приложение будет доступно на **http://localhost:3000**

## Структура проекта

```
.
├── cmd/
│   └── api/              # Точка входа приложения
├── internal/
│   ├── config/           # Конфигурация приложения
│   ├── database/         # Подключение к БД
│   ├── handlers/         # HTTP обработчики (контроллеры)
│   ├── models/           # Модели данных
│   ├── repository/       # Слой работы с БД (сгенерированный sqlc)
│   └── services/         # Бизнес-логика
├── migrations/           # SQL миграции
├── queries/              # SQL запросы для sqlc
├── .env                  # Переменные окружения
├── docker-compose.yml    # Docker композиция
├── Dockerfile            # Образ приложения
└── Makefile              # Команды для удобства
```

## API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Health check |
| POST | `/api/v1/users` | Создать пользователя |
| GET | `/api/v1/users/:id` | Получить пользователя |
| GET | `/api/v1/users` | Список пользователей |
| PUT | `/api/v1/users/:id` | Обновить пользователя |
| DELETE | `/api/v1/users/:id` | Удалить пользователя |

## Документация

- **[INSTALLATION.md](INSTALLATION.md)** - полная инструкция по установке
- **[GUIDE.md](GUIDE.md)** - руководство по разработке
- **[CONCEPTS.md](CONCEPTS.md)** - объяснение концепций Golang и Fiber
- **[API_EXAMPLES.md](API_EXAMPLES.md)** - примеры использования API
# fiber-backend
